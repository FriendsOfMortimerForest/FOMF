This folder has all the images sifted as candidates for the gallery mosaic.

So that's about 100 images.

Using the simple CSS for responsive layout, the idea is to have this simple grid of all images.

This can then be put into github fomf account and then it can be tested on mobile and desktop as a starting point that is retained as a repository.